# Em desenvolvimento, deixe vazio para usar o proxy do Vite (vite.config.ts → localhost:3001).
# Em produção, aponte para a URL pública da API, por exemplo:
# VITE_API_BASE_URL=https://api.exemplo.gov.br
VITE_API_BASE_URL=
